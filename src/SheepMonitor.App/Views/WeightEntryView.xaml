<UserControl x:Class="SheepMonitor.App.Views.WeightEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="وزن‌گیری گروهی" FontSize="28" FontWeight="Bold" Margin="0,0,0,18"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="تاریخ وزن‌گیری:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker SelectedDate="{Binding WeighedAt}" Width="160"/>
            <Button Content="بارگذاری گوسفندان" Margin="15,0,0,0" Padding="14,5" Click="Load_Click"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="شماره دام" Binding="{Binding Sheep.Number}" IsReadOnly="True" Width="150"/>
                <DataGridTextColumn Header="جنسیت" Binding="{Binding Sheep.Gender}" IsReadOnly="True" Width="120"/>
                <DataGridTextColumn Header="وزن (کیلوگرم)" Binding="{Binding WeightKg, UpdateSourceTrigger=PropertyChanged}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel Grid.Row="3">
            <TextBlock Text="میانگین وزن: " FontWeight="Bold" FontSize="18" DockPanel.Dock="Right"/>
            <TextBlock Text="{Binding AverageWeight}" FontWeight="Bold" FontSize="18" Margin="8,0,30,0" DockPanel.Dock="Right"/>
            <Button Content="محاسبه میانگین" Padding="16,7" Margin="8,0" Click="Calculate_Click" DockPanel.Dock="Left"/>
            <Button Content="ثبت وزن‌ها" Padding="16,7" Margin="8,0" Click="Save_Click" DockPanel.Dock="Left"/>
        </DockPanel>
    </Grid>
</UserControl>
