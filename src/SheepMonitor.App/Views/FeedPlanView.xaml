<UserControl x:Class="SheepMonitor.App.Views.FeedPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="مدیریت برنامه غذایی" FontSize="28" FontWeight="Bold" Margin="0,0,0,18"/>
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="نام برنامه"/><TextBox Text="{Binding PlanModel.Name, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
            <StackPanel Grid.Column="1" Margin="10,0,0,0"><TextBlock Text="گروه هدف"/><TextBox Text="{Binding PlanModel.TargetGroup, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
            <StackPanel Grid.Column="2" Margin="10,0,0,0"><TextBlock Text="شروع"/><DatePicker SelectedDate="{Binding PlanModel.StartDate}"/></StackPanel>
            <StackPanel Grid.Column="3" Margin="10,0,0,0"><TextBlock Text="پایان"/><DatePicker SelectedDate="{Binding PlanModel.EndDate}"/></StackPanel>
            <Button Grid.Column="4" Content="ایجاد برنامه" Margin="10,18,0,0" Padding="14,6" Click="SavePlan_Click"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="320"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <GroupBox Header="برنامه‌ها" Margin="0,0,12,0">
                <ListBox ItemsSource="{Binding Plans}" SelectedItem="{Binding SelectedPlan}" DisplayMemberPath="Name" SelectionChanged="Plan_SelectionChanged"/>
            </GroupBox>
            <GroupBox Grid.Column="1" Header="افزودن ماده غذایی">
                <Grid Margin="12">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Width="220"><TextBlock Text="ماده غذایی"/><ComboBox ItemsSource="{Binding Feeds}" DisplayMemberPath="Title" SelectedValuePath="Code" SelectedValue="{Binding ItemModel.FeedName}"/></StackPanel>
                        <StackPanel Width="140" Margin="10,0,0,0"><TextBlock Text="مقدار روزانه (کیلو)"/><TextBox Text="{Binding ItemModel.AmountKgPerDay, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                        <StackPanel Width="140" Margin="10,0,0,0"><TextBlock Text="واحد"/><TextBox Text="{Binding ItemModel.Unit, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                        <StackPanel Width="120" Margin="10,0,0,0"><TextBlock Text="وعده در روز"/><TextBox Text="{Binding ItemModel.MealsPerDay, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                        <Button Content="افزودن" Margin="10,18,0,0" Padding="14,6" Click="SaveItem_Click"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Text="اقلام برنامه انتخاب‌شده" FontWeight="Bold" Margin="0,16,0,8"/>
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ماده غذایی" Binding="{Binding FeedName}" Width="*"/>
                            <DataGridTextColumn Header="مقدار روزانه" Binding="{Binding AmountKgPerDay}" Width="140"/>
                            <DataGridTextColumn Header="واحد" Binding="{Binding Unit}" Width="100"/>
                            <DataGridTextColumn Header="وعده در روز" Binding="{Binding MealsPerDay}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
